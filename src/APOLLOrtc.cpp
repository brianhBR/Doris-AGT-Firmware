// APOLLOrtc removed — no implementation. The project uses platform `rtc`.
// This source file is retained as an empty placeholder to avoid build
// errors if any legacy translation units still reference it during cleanup.
