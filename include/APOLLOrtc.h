#ifndef APOLLORTC_H
#define APOLLORTC_H

// APOLLOrtc removed — the project now uses the platform `Apollo3RTC` (rtc).
// This header remains as an empty placeholder to avoid include errors
// in older builds while we clean up references. It provides no symbols.

#endif // APOLLORTC_H
